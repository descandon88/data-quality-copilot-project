[pytest]
testpaths = tests
# conftest.py also inserts the repo root into sys.path as a fallback, but
# setting it here too means `pytest` works the same way whether it's run
# via `docker compose exec app pytest` (which already has PYTHONPATH=/app
# set — see docker-compose.yml) or invoked some other way.
pythonpath = .
